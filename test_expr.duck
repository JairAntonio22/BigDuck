proc main()
var a, b, c, d int;
var e, f, g, h float;
var i, j, k, l bool;
{
    i <- true and j and (k or a < b) and not l;

    if i or k {
        b <- 100 + c;
    } else {
        c <- d + h;
        d <- a + b;
    }
}
