
#| main function |#
proc main() 
    var x, y, abs float;
{
    x <- 2;
    y <- 1;
    abs <- 1;

    loop abs > 0.0001 {
        print(y);
        y <- (x / y + y) / 2;

        abs <- y * y - x;

        if abs < 0 {
            abs <- -1 * abs;
        }
    }

    print(y);
}
